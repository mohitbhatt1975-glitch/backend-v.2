# Pinned to the versions this project was built and tested against.
# Unpinned, a breaking xgboost or pandas release fails the build or refuses
# to load thermal_model_v4.pkl -- and it happens on deploy day, not today.

fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
httpx==0.28.1
joblib==1.6.0
pandas==3.0.5
numpy==2.5.3
scipy==1.18.1
scikit-learn==1.9.1
xgboost==3.4.1
